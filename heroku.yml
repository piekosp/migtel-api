setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    api: Dockerfile
release:
  image: api
  command:
    - python manage.py migrate
    - python manage.py collectstatic --noinput
run:
  api: gunicorn api.wsgi:application --bind 0.0.0.0:$PORT
